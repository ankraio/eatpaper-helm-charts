{{- $fullName := include "eatpaper-api.fullname" . -}}
Eatpaper API has been deployed.

{{- if .Values.ingress.enabled }}
{{- if .Values.ingress.hostname }}

  URL: http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.hostname }}{{ .Values.ingress.path }}
{{- else }}

  The Ingress has no hostname configured — it will accept traffic on any host.
{{- end }}
{{- else }}

  No Ingress is enabled. To access the API from outside the cluster, run:

    kubectl port-forward svc/{{ $fullName }} 8000:{{ .Values.service.port }}
{{- end }}
